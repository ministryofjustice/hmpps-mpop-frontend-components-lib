export declare class AgentConfig {
    timeout: number;
    constructor(timeout?: number);
}
export interface ApiConfig {
    url: string;
    timeout: {
        response: number;
        deadline: number;
    };
    agent: AgentConfig;
}
